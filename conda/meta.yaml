package:
  name: hwsd_provider
  version: "0.1.1"

source:
  path:
    ../

build:
  number: 0
  noarch: python
  script: pip install .

requirements:
  host:
    - pip
    - python
  run:
    - python
    - gdal
    - pyodbc
    - rasterio

about:
  license: BSD
  license_file: LICENSE
  summary:
